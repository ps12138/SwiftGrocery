//: [Previous](@previous)

import Foundation

var str = "Hello, playground"

//: [Next](@next)

// Swift 的⽅法是⽀持默认参数的，
// - 在声明⽅法时，可以给某个参数指定⼀个默认使⽤的值。
// - 在调⽤该⽅法时要是传⼊了这个参数，则使⽤传⼊的值，
// - 如果缺少这个输⼊参数，那么直接使⽤设定的默认值进⾏调⽤。
// - Swift 中的默认参数限制更少，并没有所谓 "默认参数之后
//   不能再出现⽆默认值的参数"这样的规则

// 可以说这是 Objective-C 社区盼了好多年的⼀个特性了，
// Objective-C 由于语法的特点⼏乎⽆法在不⼤幅改动的情况下很好地实现默认参数。

